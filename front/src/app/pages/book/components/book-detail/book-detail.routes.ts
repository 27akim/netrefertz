import { Routes } from "@angular/router";
import { BookDetailComponent } from "./book-detail.component";

export const BOOK_DETAIL_ROUTES: Routes = [
    { path: '', component: BookDetailComponent },
];